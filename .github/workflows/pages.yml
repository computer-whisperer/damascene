name: pages

# Builds the damascene-web showcase bundle and deploys it to GitHub
# Pages. Fires on each published GitHub Release; `workflow_dispatch`
# also lets you redeploy manually (handy while shaking out kinks before
# the next tag).
#
# One-time setup: in the repo's Settings → Pages, set "Build and
# deployment" → Source to "GitHub Actions". Without that, deploy-pages
# fails the first run.

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Pages serves a single site, so newer deploys should win — but don't
# cancel one in flight; the deploy step is fast and cancelling mid-upload
# can leave the site half-updated.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build wasm bundle
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      # Prebuilt wasm-pack binary; `cargo install wasm-pack` from source
      # is several minutes on a clean cache.
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack

      - name: Build damascene-web-showcase (release)
        run: wasm-pack build crates/damascene-web-showcase --target web --release

      # Pages serves whatever's at the artifact root. The harness
      # imports `./pkg/damascene_web_showcase.js` relative to index.html, so we
      # stage the two as siblings.
      - name: Stage publish directory
        run: |
          mkdir -p _site
          cp crates/damascene-web-showcase/index.html _site/
          cp crates/damascene-web-showcase/favicon.svg _site/
          cp -r crates/damascene-web-showcase/pkg _site/pkg

      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    name: Deploy to Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
