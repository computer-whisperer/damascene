# damascene-web-showcase

Browser bundle for the shared Damascene `Showcase` fixture.

This crate is intentionally unpublished. The reusable browser host API
lives in `damascene-web`; this package only wires that host to
`damascene_fixtures::Showcase` for the repository demo and GitHub Pages.
